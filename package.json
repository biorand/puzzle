{
  "name": "repuzzles",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/app.min.js --minify",
    "dev": "node dev-server.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
